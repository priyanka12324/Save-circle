# SaveCircle frontend

Responsive React + TypeScript interface for the SaveCircle community-savings MVP.

## Development

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` to the FastAPI backend origin. The UI falls back to `http://localhost:8000`.

## Checks

```bash
npm run lint
npm run build
```
